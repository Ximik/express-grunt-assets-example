<meta charset="UTF-8">
<title>Test</title>
<% css.forEach(function(file) { %>
  <link rel="stylesheet" href="<%= file %>">
<% }); %>
<% js.forEach(function(file) { %>
  <script id="live-reload" src="<%= file %>">"></script>
<% }); %>